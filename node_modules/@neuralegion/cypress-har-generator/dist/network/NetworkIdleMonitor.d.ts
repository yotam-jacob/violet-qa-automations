import { NetworkRequest } from './NetworkRequest';
import type { Observer } from './Observer';
export declare class NetworkIdleMonitor {
    private readonly networkObservable;
    private startIdleTime?;
    constructor(networkObservable: Observer<NetworkRequest>);
    waitForIdle(idleTime: number): Promise<void>;
    private startIdleTimer;
    private shouldResolve;
}
