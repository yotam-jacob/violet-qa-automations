import { NetworkRequest } from '../NetworkRequest';
import type { RequestFilter, RequestFilterOptions } from './RequestFilter';
export declare class MinStatusCodeFilter implements RequestFilter {
    apply(request: NetworkRequest, { minStatusCodeToInclude }: RequestFilterOptions): boolean;
    wouldApply(options: RequestFilterOptions): boolean;
    private normalizeThreshold;
}
