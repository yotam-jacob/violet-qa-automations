import { NetworkRequest } from '../NetworkRequest';
import type { RequestFilter, RequestFilterOptions } from './RequestFilter';
export declare class CompositeFilter implements RequestFilter {
    private readonly children;
    constructor(children?: RequestFilter[]);
    apply(request: NetworkRequest, options: RequestFilterOptions): boolean;
    wouldApply(options: RequestFilterOptions): boolean;
}
