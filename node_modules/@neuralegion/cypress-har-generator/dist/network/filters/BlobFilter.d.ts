import { NetworkRequest } from '../NetworkRequest';
import type { RequestFilter, RequestFilterOptions } from './RequestFilter';
export declare class BlobFilter implements RequestFilter {
    apply(request: NetworkRequest, _: RequestFilterOptions): boolean;
    wouldApply({ includeBlobs }: RequestFilterOptions): boolean;
}
